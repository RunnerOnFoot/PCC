# use a variable like "name" and fill it with a person's name. then add some \t and \n to the start and end of the
# name. after this, print the variable with .lstrip() & .rstrip() & .strip() methods:
name = '     \t   parsa \n  '
print(name.lstrip())
print(name.rstrip())
print(name.strip())
