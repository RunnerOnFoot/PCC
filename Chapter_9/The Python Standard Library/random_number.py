"""Trying to figure out what 'Python Standard Library' is."""

# randint(): This function takes two integer arguments
# and returns a randomly selected integer between (and including) those numbers.
from random import randint

random_number = randint(1, 6)
print(random_number)
